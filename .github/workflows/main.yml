Skip to content
Navigation Menu
Mohit0135
/
movieBooking-Application

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Backend workflow
modified yml with npx to deploy #33
Jobs
Run details
Workflow file for this run
.github/workflows/main.yml at 6b4a8e4
name: Backend workflow

on: 
  push:
    branches: 
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 20

    - name: Change directory to backend
      run: cd backend

    - name: Install dependencies
      run: npm install
      working-directory: backend
  
    - run: echo "This workflow ran successfully for the particular action"
    
  deploy:
    
    needs: build

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install sshpass
      run: sudo apt-get install -y sshpass

    - name: Deploy to Droplet
      env:
        DROPLET_IP: ${{ secrets.DIGITALOCEAN_DROPLET_IP }}
        DROPLET_PASSWORD: ${{ secrets.DIGITALOCEAN_DROPLET_PASSWORD }}
      run: |
        sshpass -p "${DROPLET_PASSWORD}" ssh -o StrictHostKeyChecking=no root@${DROPLET_IP} << 'EOF'
          
          rm -rf ~/movieBooking-Application
          git clone https://github.com/Mohit0135/movieBooking-Application.git 
          git fetch origin
          git checkout main
          git pull origin main
        
          cd ./movieBooking-Application/backend
          npm install --legacy-peer-deps
          npm install --legacy-peer-deps
          npx pm2 start app.js --name movieBookingApp
          npx pm2 save
        EOF
modified yml with npx to deploy · Mohit0135/movieBooking-Application@6b4a8e4 